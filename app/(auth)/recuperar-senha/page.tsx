import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Recuperar acesso - EME",
  description: "Recuperação de acesso ao EME.",
  robots: { index: false, follow: false },
}

export default function RecoveryPage() {
  return null
}
