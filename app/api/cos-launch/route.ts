import { NextResponse } from "next/server"
import { getAuthenticatedUser } from "@/lib/auth-route"
import { routeCosLaunch } from "@/lib/cos-launch/router"
import type { CosLaunchRequest } from "@/lib/cos-launch/types"
export const dynamic = "force-dynamic"
function payload(value: unknown): CosLaunchRequest { if (!value || typeof value !== "object" || Array.isArray(value)) return {}; const input = value as Record<string, unknown>; return { message: typeof input.message === "string" ? input.message.slice(0, 3000) : undefined, action: typeof input.action === "string" ? input.action.slice(0, 80) : undefined, payload: input.payload && typeof input.payload === "object" && !Array.isArray(input.payload) ? input.payload as Record<string, unknown> : undefined } }
export async function POST(request: Request) { const auth = await getAuthenticatedUser(); if (auth.error || !auth.user) return auth.error ?? NextResponse.json({ error: "Não autenticado." }, { status: 401 }); if (!auth.user.broker) return NextResponse.json({ error: "Perfil de corretor não encontrado." }, { status: 403 }); try { return NextResponse.json(await routeCosLaunch({ brokerId: auth.user.broker.id, userId: auth.user.id, request: payload(await request.json()) })) } catch (error) { console.error("[cos-launch] request failed", { brokerId: auth.user.broker.id, error }); return NextResponse.json({ error: "Não foi possível concluir esta operação agora. Tente novamente." }, { status: 500 }) } }
