"use client"
import Link from "next/link"
import { CalendarDays, FileText, Home, UserRound } from "lucide-react"
import type { CosLaunchCard } from "@/lib/cos-launch/types"
const icons = { property: Home, client: UserRound, contract: FileText, proposal: FileText, document: FileText, agenda: CalendarDays }
export function CosLaunchCards({ cards }: { cards: CosLaunchCard[] }) { return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{cards.map((card) => { const Icon = icons[card.kind]; return <article key={`${card.kind}-${card.id}`} className="overflow-hidden rounded-[24px] border border-white/80 bg-white/85 shadow-[0_18px_50px_rgba(28,55,45,0.09)] backdrop-blur-xl">{card.imageUrl ? <img src={card.imageUrl} alt="" loading="lazy" decoding="async" className="h-32 w-full object-cover" /> : null}<div className="space-y-3 p-4"><div className="flex items-start gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-2xl bg-emerald-50 text-emerald-700"><Icon className="size-4" aria-hidden="true" /></span><div className="min-w-0 flex-1"><h3 className="truncate text-sm font-semibold text-slate-950">{card.title}</h3>{card.subtitle ? <p className="mt-1 line-clamp-2 text-xs text-slate-500">{card.subtitle}</p> : null}</div>{card.status ? <span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-medium text-emerald-700">{card.status}</span> : null}</div><dl className="space-y-1.5">{card.meta.map((item) => <div key={`${item.label}-${item.value}`} className="flex gap-2 text-xs"><dt className="text-slate-400">{item.label}</dt><dd className="min-w-0 flex-1 text-right font-medium text-slate-700">{item.value}</dd></div>)}</dl><Link href={card.href} className="inline-flex min-h-9 w-full items-center justify-center rounded-full bg-emerald-950 px-4 text-xs font-semibold text-white transition hover:bg-emerald-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 focus-visible:ring-offset-2">{card.ctaLabel}</Link></div></article> })}</div> }
