-- Personal broker logo (independent of the discontinued Agency portal): optional footer watermark
-- image on Studio IA creatives, used only when the broker uploads one.
ALTER TABLE "Broker" ADD COLUMN "logoUrl" TEXT;
